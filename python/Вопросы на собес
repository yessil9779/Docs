Что покажет ниже код?  = 2

data = {1, True, 1.0, '1'}
  print(len(data))
Примечание. Значения True и 1считаются одними и теми же значениями в наборах и рассматриваются как дубликаты:
Примечание. Значения False и 0считаются одними и теми же значениями в наборах и рассматриваются как дубликаты:
----------------------------------
В языке программирования Python существует четыре типа данных коллекций:

Список — это упорядоченная и изменяемая коллекция. Позволяет дублировать участников.
Кортеж — это упорядоченная и неизменяемая коллекция. Позволяет дублировать участников.
Set — это неупорядоченная, неизменяемая* и неиндексированная коллекция. Никаких повторяющихся участников.
Словарь представляет собой упорядоченную** и изменяемую коллекцию. Никаких повторяющихся участников.
